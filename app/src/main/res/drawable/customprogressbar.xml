<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="#C0D3B4"
                android:centerColor="#C0D3B4"
                android:centerY="1.0"
                android:endColor="#C0D3B4"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="#64a73d"
                    android:centerColor="#64a73d"
                    android:centerY="1.0"
                    android:endColor="#64a73d"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>
</layer-list>